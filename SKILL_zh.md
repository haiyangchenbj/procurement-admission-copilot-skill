---
name: procurement-admission-copilot
description_zh: 采购准入影子助手
description_en: Procurement Admission Copilot
version: 1.0.0
disable: false
agent_created: true
---

# 采购准入助手（Procurement Admission Copilot）

采购准入里两个高重复、高摩擦、但责任仍在人的环节，用影子模式、只读方式处理：

1. **供应商材料包就绪度审查**——给定供应商散落的资质材料（营业执照、资质证书、产品册、客户案例、财报摘要、合规材料），在提交准入评审前，检查材料包是否**完整、内部一致**。
2. **审批案件管理**——给定一批准入审批案件（每家供应商的请求 + 提交材料 + 评审人 + 阶段 + 截止日 + 结论），跟踪状态、卡点、缺口，让评审人能行动。

本 Skill **只结构化、只标记**，不撰写合同条款，不下准入/采购结论。结论责任留在采购委员会或采购方。

## 何时使用

- 供应商正在准备采购准入 / 供应商入库的材料包，想在提交评审前过一遍就绪度。
- 你收到供应商的材料，需要快速判断公司名称、证书、数字在跨文档间是否一致。
- 你管着一批准入审批案件，需要一张状态看板：哪些卡住了、哪些因缺材料被堵、哪些该做决策了。
- 想给评审人 / 委员会一份可一键审阅的就绪度报告或案件报告。

## 何时不使用

- 撰写合同条款、报价条款或准入的法律/采购结论 → 设计上不在范围内（"不碰合同结论"）。
- 单篇文章/报告的事实核验 → 用 `claim-to-source-auditor`。
- 对供应商做谈判或打分排序 → 本 Skill 只跟踪和标记，不排名、不决策。
- 直接修改供应商文档。

## 输入

```yaml
mode: material_package | approval_cases
# 模式 A — 材料包就绪度
supplier_inputs:        # 文件夹或文件列表
  - path:
    label: 营业执照 | 资质证书 | 产品册 | 案例 | 财报摘要 | 合规材料
checklist:              # 可选的行业标准资质清单
  - item:
    mandatory: true
reference_source:       # 以哪份材料为身份权威（默认：营业执照）
# 模式 B — 审批案件
cases:                  # CSV / JSON / 案件文件夹
  - supplier:
    submitted_materials:
    reviewer:
    stage:
    due_date:
    decision:           # pending | approved | rejected | needs-info
    notes:
review_only: true       # 绝不改写；输出结构化发现
```

## 工作流

### 模式 A — 供应商材料包就绪度审查

#### 步骤 1：[确定性] 读取并抽取结构化字段

对每份材料抽取：

- 公司法定名称（**按该材料原文记录**——差异本身就是信号）。
- 统一社会信用代码（USCC，若有）。
- 证书名称与发证 / 到期日期。
- 营收 / 规模数字及其出处。
- 产品 / 服务类目与申报范围。
- 联系与登记信息。

将抽取结果写成材料清单（manifest），供确定性脚本消费。

#### 步骤 2：[确定性] 跑就绪度检查

运行 `scripts/check_material_package.py`：

- **身份一致性**：归一化后比对各文档法定名称；任何分歧标为潜在 P0。
- **USCC 存在性**：应填却缺失 → 标 P0。
- **证书有效性**：强制证书过期 → P0；90 天内到期 → P1。
- **必交材料完整性**：缺失强制材料 → P0。
- **财务一致性**：两份营收数字偏差超过容差带 → 标 P1（需核对）。

输出 `01-readiness-check.json`。

#### 步骤 3：[LLM] 一致性与质量复核

解释并延伸确定性结论，补齐脚本不能做的判断：

- 跨文档名称不一致（营业执照 vs 产品册 vs 案例）。
- 过期或临近过期的证书。
- 财报、税务摘要、案例规模之间的营收矛盾。
- 薄弱或模板化的客户案例（无可量化结果）。
- 缺失推荐的（非强制）资质（ISO / 行业资质 / 安全许可）。
- 申报范围与产品类目不匹配。

严重度：

- **P0**：身份冲突（同一供应商不同法定名称 / USCC）；强制证书过期；缺失强制材料；应填 USCC 缺失。
- **P1**：临近过期证书；营收不一致；案例薄弱；缺失推荐资质。
- **P2**：不影响就绪度的格式 / 外观问题。

#### 步骤 4：[LLM] 产出结构化材料包 + 就绪度报告

按 `templates/supplier-material-package.md` 渲染标准化**供应商材料包**，按 `templates/readiness-report.template.md` 渲染**就绪度报告**（P0/P1/P2 清单 + 处置建议）。修复建议必须引用已提交材料，不得凭空生成。

#### 步骤 5：[确定性] 人工确认

暂停，确认 P0/P1 处置。由人工（或供应商 BD）修订材料包。本 Skill 只读。

### 模式 B — 审批案件管理

#### 步骤 1：[确定性] 读取并归一化

读取案件（CSV / JSON / 文件夹），归一化为案件 schema：供应商、提交材料、评审人、阶段、截止日、结论、备注。输出 `01-cases-normalized.json`。

#### 步骤 2：[LLM] 状态分析

- **卡住**：超期无结论 → 若涉及合规截止则 P0，否则 P1。
- **结论不一致**：同一供应商被一位评审人通过、另一位无理由驳回 → P0。
- **缺材料被堵**：因缺失强制材料而待决 → P1。
- **评审人瓶颈**：一位评审人积压过多未决案件 → P1。
- **标注 / 格式漂移**：P2。

#### 步骤 3：[LLM] 产出案件看板

按 `templates/case-dashboard.template.md` 渲染状态看板：状态分布、风险标记清单、待决策清单、给评审人的行动建议。看板只呈现人需要决策的内容，**不下结论**。

#### 步骤 4：[确定性] 人工确认

暂停，由评审人处理被标记案件。本 Skill 绝不输出通过/驳回结论。

## 硬规则

1. **只审阅不改写**：绝不修改供应商文档或案件文件，只输出结构化发现，交付后 hands-off。
2. **不碰合同结论**：绝不撰写合同条款、报价或准入/采购结论；只结构化、只标记，结论由人下。
3. **身份锚定官方登记**：营业执照 / 官方登记对法定名称与 USCC 为权威，其他材料必须与之对齐。
4. **P0 阻断就绪 / 决策就绪**：材料包有 P0 即未就绪；案件有 P0 须人工介入方可决策。
5. **不编造**：修复建议必须引用已提交材料。
6. **确定性检查是基线**：`scripts/check_material_package.py` 负责数字与一致性，LLM 层只解释与扩展，不推翻确定性结论。

## 失败处理

| 场景 | 处置 |
|---|---|
| 未提供任何供应商材料（模式 A） | 停止；至少需一份材料 |
| 材料不可读 / 非文本 | 标记后只对可读部分检查 |
| manifest 缺必填字段 | 要求补字段，不猜测身份 |
| 全部检查通过 | 报告"材料包就绪" / "无案件风险" |
| 已提交材料包中发现 P0 | 加注严重度，建议重新提交 |
| 案件无截止日 | 仅对有日期者算卡点，标注覆盖缺口 |

## 输出格式

```text
<run-dir>/
├── (模式 A) 01-readiness-check.json   # 确定性脚本输出
├── (模式 A) 02-material-package.md    # 结构化供应商材料包
├── (模式 A) 03-readiness-report.md    # P0/P1/P2 + 处置
├── (模式 B) 01-cases-normalized.json
└── (模式 B) 02-case-dashboard.md
```

## 校验

- [ ] 每份材料的法定名称都做了身份一致性比对。
- [ ] 证书到期日按参考日检查，P0/P1 判定正确。
- [ ] 必交材料按清单做了完整性检查。
- [ ] 营收偏差超容差时已标记不一致。
- [ ] 任何输出都不含合同条款或准入结论。
- [ ] 交付前记录了人工确认。

## 易错点

- 把"临近过期"当"没事"——90 天是预警线，不是及格线。
- 用"好看的数字"去调和营收矛盾——应当标记分歧，让供应商解释。
- 让看板暗示结论（"通过供应商 X"）——它只呈现，不决策。
- 默认产品册范围等于申报经营范围——范围不符是真实准入风险。
- 只用一份文档跑模式 A 就称"就绪"——完整性需要整套材料。
